# Fix for npm optional dependencies bug on Vercel
# https://github.com/npm/cli/issues/4828
# Prevents npm from removing optional dependencies needed by rollup
legacy-peer-deps=false
fund=false
audit=false
# Force include optional dependencies
optional=true
